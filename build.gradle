subprojects {

    group = 'io.github.followsclosely'
    version = '1.0.0'

    repositories {
        mavenCentral()
    }

    plugins.withType(JavaPlugin) {
        java {
            toolchain {
                languageVersion = JavaLanguageVersion.of(17)
            }
        }
    }

}
